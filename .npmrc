# 强制使用 pnpm
engine-strict=true
auto-install-peers=true

# 设置 registry（可选，使用默认的 npm registry）
# registry=https://registry.npmjs.org/

# 启用 pnpm 的严格模式
strict-peer-dependencies=false

# 设置 node_modules 位置（可选）
# node-linker=hoisted 